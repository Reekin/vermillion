# 验收实例隔离与清理

验收实例使用独立的应用数据、引擎状态与测试项目目录，不注册用户真实项目，不复制或链接其工单、调度等运行记录。真实引擎可读取必要连接配置与认证，配置中的数据库、日志等可变状态路径必须落在隔离目录；不让复制后的绝对路径继续写入用户环境。

浏览器连接绑定本次验收会话与指定实例，不自动接管用户或其他任务的浏览器。

验收成功、失败或中断后释放本任务的浏览器连接和实例，不影响用户或其他任务的实例。
