# 开发环境准备

## 要做什么

项目提供针对指定 worktree 的依赖准备 CLI 和一键 BAT，使用项目声明的包管理器和锁定依赖，安装后构建项目检查所依赖的 workspace 包产物，使新 worktree 可以直接执行项目检查命令。返回结果分别说明依赖安装与必要构建的实际状态；任一步失败不报告准备完成。桌面验收产物的准备与核对见[隔离实例准备](../Acceptance/PRD.md)。

## 明确不做什么

不影响用户正在使用的实例，不建设通用环境管理系统。
